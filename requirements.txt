# Organ Voicing Tool - Level 1 (Live Meter)
# Install with:  pip install -r requirements.txt

sounddevice>=0.4.6     # audio input capture (PortAudio); works with mic input on Windows
numpy>=1.24            # signal analysis
mido>=1.3              # MIDI message handling
python-rtmidi>=1.5     # MIDI backend for mido (sending notes to Hauptwerk)
pyautogui>=0.9.54      # keyboard automation for auto-apply (Level 3)
pyperclip>=1.8         # clipboard set/get for pasting decimal dB values
